<!--index.wxml-->
<view class="container">
  <van-field
    value="{{ value }}"
    placeholder="请输入场景描述"
    border="{{ false }}"
    bind:change="onTextChange"
  />
  <van-button size="small" type="primary" bind:click="postToGenerate">生成</van-button>
</view>